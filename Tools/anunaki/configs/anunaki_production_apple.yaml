data:
  dataset: /Volumes/AnunakiData/anunaki_dataset_512.npz
  val_fraction: 0.08
  seed: 5819
  max_train_samples:
  max_val_samples:
model:
  width: 64
  depth: 8
optim:
  epochs: 80
  batch_size: 2
  lr: 0.0002
  weight_decay: 0.0001
  grad_clip_norm: 1.0
  grad_accum_steps: 4
run:
  out_dir: /Volumes/AnunakiRuns/anunaki_production_mps
  device: auto
  sample_every_epochs: 5
  checkpoint_every_epochs: 5
  num_workers: 0
  deterministic: true
